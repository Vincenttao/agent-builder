[project]
name = "tarot-divination-agent"
version = "0.1.0"
description = "Generated by Agent Builder — Tarot divination Agent (OpenJiuwen runtime)."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
