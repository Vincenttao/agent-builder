# OpenJiuwen configuration (P0 mock mode — no real key required).
# Fill OPENJIUWEN_API_KEY / OPENJIUWEN_BASE_URL to bind the real runtime later.
OPENJIUWEN_API_KEY=
OPENJIUWEN_BASE_URL=
MOCK_OPENJIUWEN=true
